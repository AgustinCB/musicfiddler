{
  "name": "online-music-tools",
  "scripts": {
    "build": "gulp build",
    "start": "static -p 8000 site",
    "test": "standard src/js/**/*.js src/js/*.js --global ReactDOM --global React --global Audio"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "gulp": "^3.8.7",
    "gulp-autoprefixer": "0.0.8",
    "gulp-browserify": "^0.5.1",
    "gulp-sass": "2.2.0",
    "node-static": "^0.7.7"
  }
}
